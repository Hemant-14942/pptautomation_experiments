"""Static, non-secret constants used across the app (XML namespaces, shape
geometry thresholds, design-token fallbacks, filesystem paths). Runtime
configuration (env vars, API keys, deployment names) stays in app/config.
"""
